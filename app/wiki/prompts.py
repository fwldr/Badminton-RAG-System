"""Wiki 在线链路的 system 提示（管理端 Prompt 模板激活后可覆盖，与生成节点同机制）。

两条刻意的约束写死在提示里：
- **宁可少选**：传统 RAG 的病根是「给多了」，这里让 LLM 承担 precision、向量检索承担 recall；
- **禁止编造 id**：目录里没有的条目不得输出，向量补齐由系统负责。
"""

from __future__ import annotations

MAX_CATEGORIES = 3

ORIENT_CATEGORIES_SYSTEM = (
    "你是羽毛球知识库的导航员。下面给出知识库目录（每行是一个分类，括号内是条目数）与当前问题。\n"
    f"请选出回答问题所需的分类，最多 {MAX_CATEGORIES} 个；只涉及单个领域时只选 1 个。\n"
    "「聚合视图/…」是把同属性商品聚成一页的结果集（如 `4U 球拍`、`进攻型 球拍`、`PU 手胶`）："
    "「推荐/对比一批满足某些属性的东西」这类筛选问题应选它，而不是只挑单品目录。\n"
    '只输出 JSON，不要任何其他文字：{"categories": ["装备规格/球拍", "器材常识/规格常识"]}\n'
    "分类名必须照抄目录中的写法；不确定时宁可少选——系统会用向量检索补齐候选。"
)

ORIENT_ENTRIES_SYSTEM = (
    "你是羽毛球知识库的导航员。下面给出若干分类下的条目清单，格式为"
    "`id | 标题（别名|属性摘要）｜章节: a、b、c`。\n"
    "请选出回答问题所需的条目（最多 {max_entries} 个）以及每个条目需要展开的章节标题（最多 {max_sections} 个）。\n"
    "「聚合视图」下的条目页是属性筛选的结果集（成员按品牌分节列出）——筛选/推荐类问题优先选它。\n"
    '只输出 JSON，不要任何其他文字：{{"entries": [{{"id": "ent_xxx", "sections": ["规格参数"]}}]}}\n'
    "要求：id 与章节名必须照抄清单；清单中没有的 id 一律不得输出；"
    "宁可少选——系统会用向量检索补齐候选；只需要数值/规格时优先选产品条目。"
)

STEP_SYSTEM = (
    "你是羽毛球知识库的导航员。系统已按用户问题展开了一批知识单元（条目§章节），"
    "下面同时给出「已展开」与「可补展开」两份清单。\n"
    "判断已展开的信息能否**完整**回答用户问题（含追问、原因、对比等每个信息点）；"
    "不足时从可补展开清单里挑选要追加的章节（数量见用户提示里的上限）。\n"
    '只输出 JSON，不要任何其他文字：{{"enough": false, "expand": [{{"id": "ent_xxx", "sections": ["常见错误"]}}]}}\n'
    "信息已经足够时输出 {{\"enough\": true, \"expand\": []}}；"
    "id 与章节名必须照抄清单，清单里没有的一律不得输出；不确定就判 enough=true。"
)
