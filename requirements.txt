fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
python-multipart>=0.0.9   # 文件上传（管理后台）
pymysql>=1.1              # 业务库驱动：DB_BACKEND=mysql（默认后端）；测试走标准库 sqlite3，全离线

# 测试
pytest>=8.0
httpx>=0.27

# RAG 向量库与 LLM
chromadb>=1.5
openai>=3.0

# 混合检索（BM25 词法 + 向量 RRF 融合）
jieba>=0.42
rank-bm25>=0.2

# LangGraph Agent 编排（Phase 3）
langgraph>=1.0

# Wiki 条目 frontmatter 读写（app/wiki/；此前仅随 chromadb 间接安装）
pyyaml>=6.0

# Langfuse 可观测（v4：OpenTelemetry 基座，trace 由 trace_id 隐式标识，根 observation 承载 input/output）
langfuse>=4,<5

# 文档/图片入库（PDF 解析 + OCR）
pymupdf>=1.24            # fitz，PDF 文本/表格抽取（wheel ~40MB，内置 CJK）
pymupdf4llm>=0.0.17      # PyMuPDF → Markdown（含表格抽取）
rapidocr-onnxruntime>=1.3  # 图片 OCR（onnxruntime，模型 ~15MB，全离线 CPU）
